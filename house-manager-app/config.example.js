/**
 * House Manager — local config
 *
 * COPY THIS FILE to `config.js` (in the same folder) and fill in your
 * real Supabase values. The real `config.js` is gitignored so your
 * credentials never get committed to GitHub.
 *
 * Find these values in your Supabase dashboard:
 *   Project Settings → API → "Project URL" and "anon public" key
 */
window.APP_CONFIG = {
  SUPABASE_URL: 'YOUR_SUPABASE_PROJECT_URL',
  SUPABASE_ANON_KEY: 'YOUR_SUPABASE_ANON_KEY'
};
